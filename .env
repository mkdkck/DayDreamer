# .env file for the DayDreamer application

# Database Configuration
DB_USERNAME=root            # Username for the database
DB_PASSWORD=myPassword      # Password for the database
DB_NAME=daydreamer_db       # Name of the database
DB_HOST=localhost           # Database host (e.g., localhost)

# Session Configuration
SESSION_SECRET=mySecretKey  # Secret key for encrypting session cookies

# Third-party API Keys (if any)
# API_KEY=yourAPIKey        # API key for any third-party service being used

# Other configuration settings
# PORT=3000                 # Port on which the server will run (optional, defaults to 3000 in many environments)
